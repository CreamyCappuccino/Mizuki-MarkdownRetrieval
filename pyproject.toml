[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mizuki-markdown-retrieval"
version = "0.1.0"
description = "Markdown retrieval adapter for SearchE and the shared Retrieval Toolkit"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Private" }
authors = [{ name = "Mizuki / CreamyCappuccino" }]
dependencies = [
  "psycopg[binary]>=3.2,<4",
  "pgvector>=0.5,<1",
  "tomlkit>=0.13,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "mcp>=2.1.1,<2.2",
  "PyJWT[crypto]>=2.13,<3",
]
mcp = ["mcp>=2.1.1,<2.2"]
remote = [
  "mcp>=2.1.1,<2.2",
  "PyJWT[crypto]>=2.13,<3",
  "uvicorn>=0.30,<1",
]

[project.scripts]
mdr = "mizuki_markdown_retrieval.cli:main"
mdr-mcp = "mizuki_markdown_retrieval.mcp_server:main"
mdr-remote = "mizuki_markdown_retrieval.remote_cli:main"
# Compatibility aliases during the transition to the neutral OSS CLI name.
mizuki-mdr = "mizuki_markdown_retrieval.cli:main"
mizuki-mdr-mcp = "mizuki_markdown_retrieval.mcp_server:main"
mizuki-mdr-remote = "mizuki_markdown_retrieval.remote_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
